# Test: lentype - len() on non-str/list (fatal)
# The OK marker is BEFORE the error since execution stops at the error
print("=== t18_err_lentype ===")
print("=== t18_err_lentype OK ===")
x = len(42)
print(x)
